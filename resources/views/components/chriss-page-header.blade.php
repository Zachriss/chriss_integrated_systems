<div class="chriss-shared-page-header__text">
    <h1 class="chriss-shared-page-header__title">{{ $title }}</h1>
    @if ($subtitle)
        <p class="chriss-shared-page-header__subtitle">{{ $subtitle }}</p>
    @endif
</div>